% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ARBOLhelper.R
\name{dgcMatrix.aggregate}
\alias{dgcMatrix.aggregate}
\title{Efficient Aggregation of dgCMatrix Columns}
\usage{
dgcMatrix.aggregate(mat, groupings, aggregation_function)
}
\arguments{
\item{mat}{A matrix of class "dgCMatrix" to be aggregated.}

\item{groupings}{A numeric vector specifying the group for each row in `mat`.}

\item{aggregation_function}{A function to aggregate the groups. E.g., mean, sum, median.}
}
\value{
A sparse matrix of class "dgCMatrix" with aggregated columns.
}
\description{
This function efficiently aggregates the columns of a sparse matrix (dgCMatrix) based on a given grouping vector.
Various aggregation functions can be applied (e.g., mean, sum, median).
}
\examples{
mat <- Matrix(c(1, 0, 2, 0, 0, 3), nrow = 3, sparse = TRUE)
groups <- c(1, 1, 2)
dgcMatrix.aggregate(mat, groups, mean)
}
